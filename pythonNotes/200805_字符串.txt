#定义一个变量a，其值为数字1
a=1
#定义一个变量b，其值为字符串'1'
b='1'
#print()函数，是Python自带的打印函数，可以将括号内的值打印到控制台，输出其结果
#在python中，如果在某一行的开头加入一个#号，则这一行就变成了注释；注释的作用是，Python不再关注这一行代码，也不会运行
#有注释的行是给程序员/测试员看的，而不是给Python看的，注释也可以写在代码的后面
# print(1+5) #数字之间可以直接相加
# print('1'+5)#字符串和数字不可以直接相加，会报错
#int型，指的是整数类型，str型，指的是字符串类型
#在Python中，字符串既可以用单引号表示，也可以用双引号表示
# print('Hello')
# print("Hello")
#当字符串内部本身有双引号时，为避免出现语法错误，可以用单引号来表示这个字符串
# print('He said:"Hello!"')
#当字符串内部本身有单引号时，为避免出现语法错误，可以用双引号来表示这个字符串
# print("It's OK")
#转义符 \n是换行符的意思 \t是制表符的意思
# print('c:\note1.txt')
#如果想让换行符或其他有特殊含义的符号不生效，则可以在\之前再加上一个\
# print('c:\\note1.txt')
#如果一个字符串中有很多转义符或其他特殊字符，一个一个的转比较费事,可以用r来统一取消转义
# print(r'c:\n\a\r\t\b')
#在工作中，写路径时，可以使用/来代替\,这种写法在Python中是允许的
# print('c:/note1.txt')
#三引号''' ''',""" """三引号也是一种字符串，三个单引号和三个双引号都被认为是三引号，但是前后要一致
#三引号会显示字符串中的所有内容，包括你的换行和空格等
# print("""曾经沧海难为水，
# 除却巫山不是云。
# 取次花丛懒回顾，
# 半缘修道半缘君。""")
#三引号也可以用来作注释，一般是用在函数以及方法里。
# print(print.__doc__)
#字符串的拼接
# print('1'+'5')
#字符串和数字相乘，表示该字符串显示几次
# print('a'*6)


#字符串的下标
str1='abcdefggugyuguihljlkljl好久好久发货就返回结果韩剧韩剧kmkmknoho'
#字符串的下标是从0开始
# print(str1[2]) #打印c
# print(str1[6]) #打印g
# print(str1[-1]) #也是打印g，从后往前开始算下标，最后一位是-1，倒数第二位是-2，以此类推
#字符串中的切片 [起始位置:终止位置]，切片包含起始位置本身，但不包含终止位置本身
# print(str1[2:4])
#当切片想要包含最后一位时，第一种做法，将切片的终止值设定的大于最大下标，这种做法只适合字符串
#第二种做法，也是常规办法，不取值
# print(str1[2:])
#len()函数可以返回字符串的长度
# print(len(str1))
#字符串的最大下标与len()的关系，始终是最大下标=len(字符串)-1
#index()方法可以返回某个字符在字符串中的下标
# print(str1.index('e'))
#字符串属于不可变对象，列表是一个可变对象，也就是说我们不可以通过下标来修改字符串中的某个字符
# str1[0]='q' #这么写代码会报错
#如果要修改字符串，只能全部改，即重新赋值
# str1='qq'
# print(str1)